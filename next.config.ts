import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: "export",
  // trailingSlash: true,
  // basePath: "https://fissored.github.io/fatture/"
  basePath: "/fatture",
};

export default nextConfig;
